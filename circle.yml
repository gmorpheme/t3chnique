machine:
  pre:
    - pip install awscli
    - aws s3 cp --region eu-west-1 s3://private.curvelogic.co.uk/profiles.clj ~/.lein/profiles.clj

deployment:
  snapshot:
    branch: master
    commands:
      - lein deploy curvelogic-snapshots
